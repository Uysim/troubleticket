= link_to edit_user_path(user), class: 'btn btn-default btn-xs' do
  Edit
= link_to user_path(user), method: :delete, data: { confirm: 'Are you sure you want to delete?' }, class: 'btn btn-danger btn-xs' do
  Delete
